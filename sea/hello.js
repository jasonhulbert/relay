// Throwaway SEA smoke-test entry (M0). Node SEA is CommonJS-only, so this is a
// plain CJS file. It exists only to prove a self-contained single binary runs;
// it is not part of the real spine.
console.log('hello');
