// The read-only local web view: a render of `.relay/` for operator supervision
// and the human decision inbox (design §3.11). Distinct from Surfaces/visual
// verification. Placeholder until the web view milestone (M5).
export {};
