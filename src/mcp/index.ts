// MCP as the universal capability bus (C9): the spine as MCP client for the
// orchestrator's own judgment calls, author of first-party servers, and
// lifecycle manager of the server fleet (design §9.4). Placeholder until the
// milestones that grant real tools.
export {};
